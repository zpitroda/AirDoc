# Nginx Reverse Proxy Configuration for AirDoc (airdochealth.com)
# Place in /etc/nginx/sites-available/airdochealth.com and symlink to /etc/nginx/sites-enabled/

server {
    listen 80;
    listen [::]:80;
    server_name airdochealth.com www.airdochealth.com;

    # Redirect all HTTP traffic to HTTPS (handled automatically by certbot or manual)
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name airdochealth.com www.airdochealth.com;

    # SSL Certificates (provisioned via: certbot --nginx -d airdochealth.com -d www.airdochealth.com)
    # ssl_certificate /etc/letsencrypt/live/airdochealth.com/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/airdochealth.com/privkey.pem;

    # Security Headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # Client upload limits
    client_max_body_size 10M;

    # Proxy to Next.js App running on port 3000
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Static Assets Cache Optimization
    location /_next/static {
        proxy_pass http://127.0.0.1:3000;
        proxy_cache_valid 200 365d;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }
}
